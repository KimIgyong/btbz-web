# btbz CMS nginx 스니펫 — 기존 server 블록에 추가 (Ubuntu, nginx 1.18)
# ⚠️ 적용 전 반드시 기존 www.btbz.ai 설정/웹루트를 확인할 것 (REQ Q-3)

# ── modora.btbz.ai server 블록에 추가 ──────────────────────────
#   /api → 백엔드 프록시 (같은 오리진이라 CORS 불필요)
#
#   location /api/ {
#       proxy_pass http://127.0.0.1:3100;
#       proxy_set_header Host $host;
#       proxy_set_header X-Real-IP $remote_addr;
#       proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#       proxy_set_header X-Forwarded-Proto $scheme;
#   }

# ── www.btbz.ai server 블록에 추가 ─────────────────────────────
#   기존 루트(/) 콘텐츠는 그대로 두고 /admin 과 /api 만 추가
#
#   location /admin {
#       alias /var/www/btbz-web/admin;      # 저장소의 admin/ 배포 경로에 맞게 수정
#       index index.html;
#       try_files $uri $uri/ /admin/index.html;
#   }
#
#   location /api/ {
#       proxy_pass http://127.0.0.1:3100;
#       proxy_set_header Host $host;
#       proxy_set_header X-Real-IP $remote_addr;
#       proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#       proxy_set_header X-Forwarded-Proto $scheme;
#   }
